{
  "name": "ptools-core",
  "version": "0.1.0",
  "description": "Open-source core logic for Private Tools — pure functions, no UI, fully auditable",
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^5.7.0"
  },
  "license": "MIT"
}
